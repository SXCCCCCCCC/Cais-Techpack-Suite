org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=false

minecraft_version=1.20.1
minecraft_version_range=[1.20.1,1.21)
forge_version=47.4.10
forge_version_range=[47,)
loader_version_range=[47,)

mapping_channel=official
mapping_version=1.20.1

mod_id=iffermentfix
mod_name=IF Fermentation Fix
mod_license=MIT
mod_version=1.0.0
mod_group_id=com.sxcccccccc.iffermentfix
mod_authors=SXCCCCCCCC
mod_description=Fixes Industrial Foregoing Fermentation Station voiding its output fluid (while still consuming input ore meat + catalyst) when the output tank already holds fermented ore meat with a different NBT tag. canIncrease now also requires the output tank NBT to match the input, so the machine halts instead of swallowing the product.
